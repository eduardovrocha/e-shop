# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new e-shop project in initialization phase. The repository skeleton exists but no source code has been added yet.

## Repository Structure

```
e-shop/
├── deploy/     # Infrastructure and deployment configuration
├── docs/       # Project documentation
└── source/     # Application source code
```

## Setup

This file will be updated with build, test, and run commands once the tech stack is established. Update this file when:
- A framework/language is chosen (add install and run commands)
- A test suite is configured (add how to run all tests and a single test)
- Linting/formatting tools are added (add the commands)
- Docker or CI is configured (add relevant commands)
