VITE_API_URL=http://localhost:3091/api/v1
VITE_APP_NAME=Andrequicé
VITE_DASHBOARD_URL=http://localhost:3092
# Stripe publishable key is fetched at runtime from GET /api/v1/stripe/config.
# Configure credentials via /admin/stripe in the dashboard.
