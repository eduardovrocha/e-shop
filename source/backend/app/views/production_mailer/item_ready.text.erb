<%= @store_name %> — <%= @last_item_pending ? "Pedido pronto" : "Peça pronta" %>
==========================================

Olá <%= @order.customer_name.to_s.split.first.presence || "cliente" %>,

<% if @last_item_pending %>
Todas as peças do seu pedido estão prontas e serão enviadas em breve.
Você receberá o código de rastreamento assim que o pedido for despachado.
<% else %>
Sua peça <%= @product&.name %><%= @order_item.size ? " — Tamanho #{@order_item.size}" : "" %> ficou pronta. Outras peças do seu pedido ainda estão em produção; você receberá outro e-mail assim que todas estiverem prontas para envio.
<% end %>

Acompanhe seu pedido em:
<%= @public_url %>

Pedido: <%= @order.number %>

---
<%= @store_name %> — <%= ENV.fetch("SUPPORT_EMAIL", "suporte@andrequice.com.br") %>
