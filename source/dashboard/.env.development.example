# ─── Dashboard — variáveis de desenvolvimento local ──────────────────────────
# Copiar para .env.development (não commitado).
# Usado pelo Vite ao rodar em modo development.
# ─────────────────────────────────────────────────────────────────────────────

# URL relativa — funciona via nginx do Docker Compose (http://localhost/api/v1)
# Para acesso direto sem nginx, usar: http://localhost:3091/api/v1
VITE_API_URL=/api/v1

VITE_APP_NAME=Andrequicé Admin
